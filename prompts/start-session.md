# Prompt: Start Session

Before answering, read:

1. `index.md`
2. `AGENTS.md`

Your first visible tool command should read `index.md`. Do not answer from memory or from a previous chat context.

Then reply with the `VOICE_NOTES_INDEX_VERSION` from `index.md` and a one-sentence summary of the current vault state.

After that, answer my request using the vault read order from `index.md`.
