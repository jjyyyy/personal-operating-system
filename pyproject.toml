[project]
name = "personal-operating-system"
version = "0.1.0"
description = "Local personal operating system workflow for an Obsidian-style LLM wiki."
requires-python = ">=3.11"
dependencies = [
  "certifi>=2025.11.12",
]

[tool.uv]
package = false
