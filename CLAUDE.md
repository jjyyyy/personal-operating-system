# Claude / Claudian Entry Point

This vault uses `index.md` and `AGENTS.md` as the source of truth.

## Required Start

Before answering or editing anything:

1. Read `index.md`.
2. Read `AGENTS.md`.
3. Reply with the `VOICE_NOTES_INDEX_VERSION` from `index.md` when asked for a context check.

Do not answer from memory or from an old chat context.

## Context Marker

The current context marker is defined in `index.md`.

If you cannot report that marker exactly, reread `index.md` before proceeding.

## Rule

Do not duplicate or reinterpret the full rules here. Follow `AGENTS.md` for behavior, read order, and maintenance commands.
